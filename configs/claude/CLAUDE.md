# CLAUDE.md

**Rule organization:** All rules are grouped by topic, stored as individual `.md` files in `~/.claude/shared/`, and imported into this main file below. When adding a new rule, place it in the topically appropriate file (or create a new one) and add an `@` import here — do not write rule content directly into this file.

@~/.claude/shared/behavioral_guidelines.md
@~/.claude/shared/working_with_repositories.md
@~/.claude/shared/implicit_requests.md
